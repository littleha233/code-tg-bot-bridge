{
  "name": "tg-codex-agent",
  "version": "0.2.0",
  "private": true,
  "description": "Telegram + Codex natural language project assistant.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npm run build && node dist/index.js",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "launchd:install": "node scripts/install-launchd.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "execa": "^9.6.0",
    "https-proxy-agent": "^7.0.6",
    "socks-proxy-agent": "^8.0.5",
    "telegraf": "^4.16.3",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
